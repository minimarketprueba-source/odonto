# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> El usuario prefiere respuestas en español. No es programador: prefiere que Claude implemente, pruebe y publique los cambios por él, y explicaciones claras sin jerga.

**Sanidad ISEPOL — Citas Médicas**: sistema de citas médicas de la Sanidad Policial (pacientes, agenda, historia clínica, recetas, reportes). SPA React desplegada en Vercel sobre Supabase. Reemplaza a una vieja app Laravel (`saas_citasmedicas`) que ya fue jubilada. Nació como clon del esqueleto de **Control-Peso** (`..\Control-Peso`) y comparte con él el proyecto Supabase, Auth y convenciones.

## Estado actual (2026-07-20) — LEER PRIMERO

- **App en vivo**: https://sanidad-citas.vercel.app · **GitHub**: https://github.com/seccioneeff-lab/sanidad-citas (privado, `seccioneeff-lab`)
- **Deploy**: push a `main` → Vercel despliega solo. La conexión a Supabase está **hardcodeada como default** en `src/lib/supabase.ts` (`DEFAULT_SUPABASE_URL` / `DEFAULT_SUPABASE_ANON_KEY`), así que NO se necesitan env vars en Vercel. La anon key es pública por diseño (la protege el RLS). Orden de config: localStorage > env vars > default.
- **Fases 0-3 COMPLETAS**: F0 esqueleto+auth · F1 pacientes+agenda · F2 consultas/historia clínica+recetas · F3 reportes+lista de espera+mantenimiento. F4 (jubilar Laravel) hecha: la carpeta `saas_citasmedicas` fue borrada. Descartados a propósito: facturación, aseguradoras, mensajería, multi-clínica.
- **Datos en Supabase**: **897 fichas de paciente, 756 activas** (600 cadetes en curso + 76 oficiales + 80 suboficiales; 141 cadetes egresados quedan `activo=false` — cada año egresa una promoción y solo una parte se queda en la Academia) · **24 profesionales reales cargados** (2026-07-20, desde "COMISIONADOS LISTA DE REVISTA.pdf" del 06/07/2026: 5 Medicina General, 6 Odontología, 7 Psicología, 2 Obstetricia, 2 Nutrición, 2 Fisioterapia; Enfermería y ayudante quedaron fuera a propósito — no manejan agenda) · 0 citas, 0 consultas (arranca en limpio) · catálogos: 14 especialidades (10 originales + Psicología/Obstetricia/Nutrición/Fisioterapia) + 14.270 códigos CIE-10.
- **Usuarios reales creados** (2026-07-20): 32 cuentas `@sanidad-citas.local` con contraseñas temporales únicas — 24 profesionales (rol `medico`, `medicos.user_id` vinculado por cédula) + 8 de enfermería (rol `recepcion` con permisos extendidos: también `consultas` y `recetas` ver/editar, para registrar a nombre de las médicas). Esquema de email: inicial+apellido (ej. `llopez@`). `user_roles.status='Activo'`. Las 6 cuentas genéricas `medico1..3`/`recepcion1..3` fueron eliminadas. Verificado end-to-end por API: login OK y RLS deja leer pacientes/medicos/ficha propia. Las credenciales quedaron en `Escritorio\Credenciales_Sanidad.txt` (el usuario debe repartir y borrar).

- **Mejoras de agenda estilo PY HIS** (2026-07-20, pedidas por las enfermeras mostrando el sistema del MSPBS `hdl.mspbs.gov.py`; fotos en `Downloads\WhatsApp Image 2026-07-20*.jpeg`): flujo Admitir (check-in con `orden_llegada` por médico/día, `admitida_at`), estado nuevo `admitida`, `citas.agendado_por` (email), Reagendar, turno derivado de la hora (Mañana/Tarde/Noche), vista Día/Mes con buscador, imprimir (`#agenda-imprimible` + `.no-print` en globals.css), sala de espera por orden de llegada, panel "Mi día" en Dashboard, página Horarios (`horarios_medicos`/`ausencias_medicos`, aviso al agendar fuera de horario o en ausencia), página Mi perfil (cambio de contraseña + teléfono de ficha propia, requiere policy `sanidad_medicos_update_propio`).

- **Feedback de la Dra. Lopez Irala** (2026-07-20, fotos `Downloads\WhatsApp Image 2026-07-20 at 11.*.jpeg`, chat de WhatsApp): estado de cita `no_acudio` (botón UserX en agenda — evita el truco PY HIS de consulta Z71.9 para ausentes), CIE-10 **obligatorio** en consulta-form, reposo con **cantidad de días** calculada/editable (días↔fecha sincronizados, ambos inclusive), filtro **por servicio** (especialidad) en la historia clínica.

- **Pacientes externos** (2026-07-26): la tabla `pacientes` ya NO es solo de cadetes. Tipos nuevos `policia` y `civil` (+ `familiar` que ya existía); `labelTipoPaciente()` / `esPacienteExterno()` en `src/api/pacientes.ts`. Los 24 médicos ahora tienen `pacientes:["ver","editar"]` (aplicado por API admin sobre `user_roles`, preservando el resto de sus permisos); **dar de baja pasó a `canDelete`** = solo admin. `documento` es opcional para familiar/civil (menores sin CI) → los impresos usan `idPaciente()` = documento o `P<id>`. Alta rápida "Registrar paciente nuevo" en el estado vacío de los buscadores de Pacientes, Lista de espera y Agendar cita (`PacienteForm` acepta `onCreated` + `busquedaInicial`). Anti-duplicados: aviso en el form comparando con `normalizeDocumento()` + botón bloqueado + `retry: 0` en `useCreatePaciente` (el retry global de React Query creaba fichas dobles) + índice único parcial en la BD. **OJO**: todo script que vincule cadetes↔pacientes debe filtrar `WHERE tipo = 'cadete'`.

- **Padrón de oficiales y suboficiales** (2026-07-27): el padrón tenía SOLO los 750 cadetes; buscar a un oficial no daba nada. Se cargaron desde `public.oficiales` (la tabla de Control-Peso alimentada por la Lista de Revista de Talento Humano ANP) → 76 oficiales + 80 suboficiales, `tipo` = `categoria`, `unidad` ← `cargo`. La tabla guarda el nombre entero en un campo y `pacientes` lo quiere partido: el corte usa un **corpus de nombres de pila** (las 2 primeras palabras de los nombres de 4 palabras) para decidir los de 3 palabras (`DIEGO JOSE SOSA` → 1 apellido vs `HECTOR VILLALBA GIMENEZ` → 2), más una regla de partícula para `ROMERO DE BRITEZ` / `ROJAS DA ROSA`. Verificado contra los 156 registros antes de aplicarlo. **9 oficiales ya tenían ficha de cadete egresado**: se convirtió la ficha existente (`tipo`, `grado`, `unidad`, `activo=true`) en vez de duplicarla, con guarda `AND NOT p.activo` para no tocar nunca a un cadete en curso. Se amplió el CHECK `pacientes_tipo_check` a los 9 tipos. En el personal de la Academia `unidad` = **cargo**, y la tarjeta/el form lo etiquetan "Función"; en un `policia` externo sigue siendo su dependencia.

- **Salvoconductos** (2026-07-27): tabla `salvoconductos` + `src/api/salvoconductos.ts` + `salvoconducto-form.tsx` + `imprimirSalvoconducto()` en `src/lib/imprimir.ts`. Autoriza el traslado del paciente fuera de la Academia (Hospital de Policía "Rigoberto Caballero", estudios u otro) con número correlativo `SC-00001`, urgente/programado, acompañante, retorno previsto y QR. Lo expide quien tenga `consultas:editar` (médicos y enfermería), desde la ficha del paciente y desde la agenda. En `DESTINOS_SALVOCONDUCTO`, `label` = texto corto de la lista y `oficial` = nombre completo del impreso.
- **Anulación de registros médicos** (2026-07-27, solo admin): columnas `anulada_at`/`anulada_por`/`motivo_anulacion` en `consultas` y `recetas`; `src/api/anulaciones.ts` + `anular-dialog.tsx` + pestaña "Anuladas" en historia-clinica-dialog. **El candado real es el trigger `trg_control_anulacion`** (función `fn_control_anulacion`, SECURITY DEFINER): rechaza cualquier UPDATE de quien no sea admin y sella `anulada_at`/`anulada_por` desde el servidor — esconder el botón no alcanza porque la anon key es pública. Efecto colateral: **un médico ya no puede editar su propia consulta**, debe pedirlo al admin. **Las 3 vistas `sanidad_*` filtran `anulada_at IS NULL`** (en `sanidad_resumen_cadete` el filtro va en el LEFT JOIN, no en un WHERE, para no perder cadetes sin consultas): anular un reposo libera al cadete de la actividad física en Control-Peso. `fetchAtenciones` filtra en la BD (no en memoria: el `.limit(10000)` descartaría atenciones reales). El nº de receta NO excluye anuladas (reutilizaría números ya impresos). Verificado end-to-end el 2026-07-27: 13 controles, incluido que un médico recibe 400 al intentar anular.

- **Enfermería / observación** (2026-07-27): módulo `/enfermeria` (pantalla de otra sesión, commit 7e5e66a) **reescrito**: guardaba todo en `localStorage`, o sea que cada computadora tenía su propia lista de internados y la enfermera del turno siguiente no veía nada. Ahora usa tablas reales `salas`/`camas`/`internaciones`/`signos_vitales` con RLS `sanidad_*`. **La BD garantiza** (índices únicos parciales `internaciones_una_por_cama` y `internaciones_un_paciente_activo`) que no haya dos pacientes en una cama ni un paciente en dos camas — devuelve 409. Los CHECK de `signos_vitales` rechazan valores fuera de rango humano (temp 30-45, spo2 50-100, etc.). El estado ocupada/libre de una cama **se deriva** de si tiene internación activa, no es una columna. `moduleKey: "consultas"` en ruta y menú (antes cualquier cuenta entraba). **Camas reales: 6, en una sola sala con mamparas** — las 4 salas y 12 camas originales eran inventadas. Verificado end-to-end el 2026-07-27: 12 controles, incluida la visibilidad entre dos cuentas distintas.

- **Avisos de exención de actividad física** (2026-07-27): trigger `trg_avisar_exencion` / `fn_avisar_exencion` (SECURITY DEFINER) en `consultas` → al guardar un reposo avisa a **todos los admins activos**. **OJO: hay DOS tablas de avisos en la base compartida** — `notificaciones` (español, era de la plantilla, estaba vacía y sin uso: la campanita de Sanidad, `src/api/notificaciones.ts` + `notifications-dropdown.tsx`, que estaba muerto) y `notifications` (inglés, la campanita de Control-Peso, columnas `leida`/`prioridad`/`metadata`). El trigger escribe en las dos; el de Control-Peso apunta a `/actividad-fisica`. **Todo el bloque de aviso va dentro de un `BEGIN … EXCEPTION WHEN others THEN RAISE WARNING`**: un aviso que falle NUNCA debe impedir que se guarde una consulta. RLS de `notificaciones`: 4 políticas, una por acción, todas `user_id = auth.uid()`.
- **Internación = exención** (2026-07-27): `sanidad_reposos_cadete` se rehízo con `UNION ALL` sumando las internaciones activas (`(-i.id)::integer AS consulta_id` para no chocar con las consultas, `reposo_tipo='local'`, especialidad 'Enfermería'). **Hay que DROP + CREATE, no `CREATE OR REPLACE`** (error 42P16: no deja cambiar el tipo de `diagnostico` de varchar(191) a text) **y reponer `GRANT SELECT … TO authenticated`**, si no Control-Peso se queda sin datos. Control-Peso agrupa por `cadete_id`, nunca por `consulta_id` (`src/api/aptitud.ts`), por eso el UNION es seguro.

- **Ficha de RAC / urgencias** (2026-07-27, pedida con foto del formulario en papel): tabla `fichas_rac` + `src/api/rac.ts` + `src/components/rac/rac-form.tsx` + página `/rac` (`moduleKey: "consultas"`). Reproduce el papel en 4 etapas con hora propia cada una: **1 Admisión** (datos del paciente) · **2 Enfermería** (PA/FC/FR/SpO2/T°, motivo, discriminante y **triaje de 4 niveles MSPBS** rojo/amarillo/verde/azul) · **3 Médico** (patología previa, alergias, examen, laboratorio, radiología, CIE-10 obligatorio, tratamiento, evolución, plan) · **4 Destino** (alta / parte sin servicio / enfermo local / reposo domiciliario / internación, con días). Flujo por etapas: enfermería abre la ficha (`estado='espera'`, ordenada por gravedad y luego por hora de llegada) y el médico la cierra. **Al cerrar se crea la consulta en `consultas`** — a propósito, para no partir la historia clínica y para que el reposo siga alimentando la exención de Control-Peso; enfermo local → `local`, reposo domiciliario e internación → `local`/`domiciliario`, **parte sin servicio NO genera reposo** (decisión a revisar con el usuario). Si el destino es internación se crea antes la `internacion` (es lo que puede chocar por cama ocupada) y el id queda en un `useRef` para que un reintento no la duplique. La consulta se crea ANTES de marcar la ficha cerrada: si falla, la ficha queda en espera y se reintenta. `imprimirFichaRac()` en `src/lib/imprimir.ts` saca el papel con las casillas marcadas. Tests del conteo inclusivo de días en `src/api/__tests__/rac.test.ts`.

- **Ancho de los diálogos — trampa recurrente** (2026-07-27): el `DialogContent` de la plantilla traía `sm:max-w-lg`. tailwind-merge **no ve conflicto entre variantes distintas**, así que un `max-w-4xl` del que llama no lo pisaba y la media query ganaba siempre: **todos** los modales (salvoconducto, receta, historia clínica, RAC, paciente) se mostraban a 512px con el contenido encimado. Se cambió a `w-[calc(100%-2rem)] max-w-lg` **sin prefijo** (el margen en pantallas chicas lo da el `w-`, no un segundo `max-w-`, que también entraría en conflicto). Test de regresión en `src/components/ui/__tests__/dialog.test.tsx`. Mismo tipo de bug que el `w-fit` del `SelectTrigger`: **antes de tocar un layout, revisar si la clase la está pisando el componente base de `ui/`**.

- **`Combobox` — desplegable con buscador** (2026-07-27, `src/components/ui/combobox.tsx`): Popover + cmdk. Filtra con `matchTexto` y `shouldFilter={false}` (el filtro propio de cmdk exige el orden exacto; así "lopez irala" encuentra "IRALA LÓPEZ"). Acepta `detalle` (segunda línea) y `buscarPor` (texto oculto por el que también busca, ej. el nombre oficial del destino del salvoconducto). Ya se usa en salvoconducto (destino, profesional) y ficha de RAC (médico, destino, cama). Tests en `src/components/ui/__tests__/combobox.test.tsx`, **incluido que funcione dentro de un `Dialog`** (Radix atrapa el foco ahí adentro). Para los tests de Radix hacen falta los mocks de `hasPointerCapture`/`scrollIntoView`.

- **Quién firma el registro** (2026-07-27, `src/components/consultas/medico-selector.tsx`): el campo "Médico" dejaba elegir a cualquiera de los 24, así que un profesional podía firmar una consulta a nombre de otro. Ahora `MedicoSelector` mira `medicos.user_id` de la cuenta logueada (`useMiMedico`): si tiene ficha propia el campo queda **fijo** en esa persona y **pisa lo que venga precargado** (el médico de la cita de la agenda). Las cuentas sin ficha (enfermería y admin) siguen eligiendo de la lista, porque cargan a nombre de las profesionales. En `disabled` (ficha de RAC ya cerrada) NO pisa nada: hay que seguir viendo a quien firmó. Se aplicó a consulta, receta, salvoconducto y RAC. **Es solo un candado de pantalla** — la anon key es pública, el candado real sería un trigger como `trg_control_anulacion`. Tests en `src/components/consultas/__tests__/medico-selector.test.tsx`.

- **Destino del paciente en la consulta** (2026-07-27): el desplegable de reposo tenía 3 opciones; ahora son los **mismos 5 destinos del papel de RAC** (`DESTINOS_ATENCION` en `src/api/consultas.ts`, que pasó a ser la única fuente — `rac.ts` la reexporta como `DESTINOS_RAC`). Columna nueva `consultas.destino` (migración `Escritorio\SQL_Destino_Consulta.txt`; **el código no puede publicarse antes de aplicarla**, PostgREST rechaza la columna desconocida y no se guardaría ninguna consulta). **Decisiones del usuario**: "parte sin servicio" **sí exime** de educación física (pasó a `reposo_tipo='local'`, también en la ficha de RAC, que antes no eximía) y "internación" desde una consulta común **interna de verdad**: pide cama libre, crea la `internacion` ANTES de la consulta (es lo único que puede chocar por cama ocupada) y guarda el id en un `useRef` para que un reintento no la duplique. `reposo_tipo` sigue siendo lo que lee Control-Peso; `destino` es el detalle fino para el impreso y la historia clínica. Los textos de cada documento viven en `DOCUMENTOS_REPOSO` de `src/lib/imprimir.ts` (`documentoReposo()` cae de vuelta en `reposo_tipo` para las consultas viejas sin `destino`).

- **Pantalla de Usuarios: roles y módulos equivocados** (2026-07-28): `Usuarios.tsx` seguía con las listas de la plantilla de Control-Peso — roles `admin`/`analyst` (ni `medico` ni `recepcion`) y módulos `cadetes`/`seguimiento`/`ergometria`/`gestion_imc`/`respaldo`/`configuracion`. Como el diálogo **escribe `user_roles.permissions` entera**, tocar el rol de una profesional y guardar la dejaba en `analyst` (fuera de `ROLES_SANIDAD` → pantalla "Sin acceso") con permisos de módulos inexistentes; y no había forma de conceder `nutricion`. Ahora `auth-context` exporta la **única fuente**: `MODULOS_SANIDAD` (9 módulos, incluye `recetas`, que no tiene ruta pero sí `hasPermission`), `ROLES_SANIDAD_OPCIONES` y `DEFAULT_PERMISOS_SANIDAD` (con entrada `admin` = todo). Extras: los roles de la otra app solo se pueden **filtrar**, no asignar (la tabla `user_roles` es compartida); sin fila de rol se muestra "Sin rol asignado" en vez de "Solo lectura"; Guardar exige rol válido y al menos un módulo (salvo admin); `permissions = {}` cuenta como vacío y cae en los del rol; y al crear una cuenta se **reescribe `user_roles`** después de la Edge Function (que es de Control-Peso y deja `permissions` en `{}`). Test de regresión `src/context/__tests__/permisos-modulos.test.ts`: lee los `moduleKey` de `App.tsx` y del sidebar y exige que existan en `MODULOS_SANIDAD`.

## Pendientes (lo que sigue)

0. **Revisión del 2026-07-28, hallazgos NO corregidos** (el usuario eligió arreglar solo la pantalla de Usuarios): (a) el módulo **Nutrición no lo ve nadie** salvo admin — las 24 cuentas tienen `permissions` explícito sin la clave `nutricion`; se arregla desde la pantalla de Usuarios ya corregida. (b) `src/api/nutricion.ts` y `pesada-dialog.tsx` **fabrican datos clínicos**: altura 170 por defecto (IMC falso), `egs` "A" y `dx_bia` "Normal" sin medición, DX BIA deducido de cintura > 90 con el **mismo umbral para ambos sexos**, `situacion_medica` fija en "APTO · puede realizar actividad física" (contradice el reposo vigente) y fecha de pesada = hoy si falta. (c) el **%MM se guarda en `pesadas.masa_muscular_kg`**, que en Control-Peso son kilos → ensucia la otra app. (d) el form de pesada **precarga el peso anterior** (riesgo de registrar el peso viejo con fecha de hoy). (e) "✅ Peso Saludable" también en bajo peso (solo compara contra el máximo). (f) botones decorativos (Exportar, Importar CSV, Nuevo Cadete, Editar, Eliminar) y "Cierre de año" con un cartel que afirma algo falso. (g) `sanitizePlainText` colapsa `\s+` → las notas de consulta **pierden los saltos de línea** aunque el impreso usa `white-space:pre-wrap`. (h) `fetchCadetesNutricion` baja todos los cadetes con todas sus pesadas (`select("*, pesadas(*)")`) y ordena en memoria. (i) **sin verificar**: si una nutricionista puede escribir en `cadetes`/`pesadas` — son tablas de Control-Peso y no tienen políticas `sanidad_*`.

0. (Migración de pacientes externos EJECUTADA y verificada end-to-end el 2026-07-26: columna `familiar_de` ✓, índice único `pacientes_documento_unico` ✓ — la BD devuelve 409 ante cédula repetida —, alta de familiar y de policía probadas en producción con cuenta de médica ✓.)

0. Ideas PY HIS aún no hechas (decidir con el usuario): PDF de consulta/historial, estudios solicitados, procedimientos con historial, preconsulta (signos vitales), historial de medicamentos prescriptos. (Migración de agenda EJECUTADA y verificada end-to-end el 2026-07-20: cita+admisión+no_acudio+horarios probados en producción con cuenta de enfermería.)

1. **Correos reales**: cuando los profesionales tengan correo real, renombrar las cuentas `@sanidad-citas.local` (hoy no pueden recuperar contraseña por email; si alguien la olvida, resetearla por API admin).
2. **Guard en Control-Peso**: que roles medico/recepcion no entren a su Dashboard (su auth-context asigna 'analyst' por defecto solo si no hay rol; los médicos tienen rol, verificar ProtectedRoute).
3. (Cerrados 2026-07-20: carga de 24 profesionales ✓, 32 cuentas creadas y vinculadas ✓, genéricas eliminadas ✓. "Karen Elizabeht" quedó tal cual el PDF por decisión del usuario.)

## Comandos

```bash
npm run dev          # Vite dev server (puerto 5173)
npm run build        # tsc && vite build (type-check bloquea el build)
npm run test:run     # Vitest una corrida
npm run lint         # eslint --max-warnings 0 (hay warnings heredados de la plantilla; lint solo archivos tocados)
npm run type-check   # tsc --noEmit
```

Herramientas locales confirmadas: Node v24, npm 11, git 2.53, VS Code 1.121.

## Contexto técnico clave

- **Supabase compartido** con control de peso: ref `vnkstlvqzkhdfeoqskcf` (región us-west-2). Credenciales de la BD (para scripts de admin por SQL directo) NO están aquí; la anon key sí está en `src/lib/supabase.ts`. Para operaciones de admin sobre la BD se usó PHP de Laragon + PDO pgsql desde la otra máquina; pedir credenciales al usuario si hacen falta.
- **Flujo probado para cargas admin sin credenciales** (2026-07-20, así se cargaron los 24 profesionales): dejar un `.txt` con el SQL en el Escritorio y que el usuario lo pegue en el SQL Editor del dashboard (`https://supabase.com/dashboard/project/vnkstlvqzkhdfeoqskcf/sql/new`) y presione Run; terminar el script con un SELECT de verificación para que el usuario pegue el resultado en el chat. Hacer los INSERT idempotentes (WHERE NOT EXISTS). La lista de revista original está en `Downloads\Documentos\Info_para_ia\COMISIONADOS LISTA DE REVISTA.pdf`.
- **Ojo con el SQL Editor**: NO usar tablas temporales (cada sentencia puede correr en conexión distinta: "relation does not exist") ni confiar en BEGIN/COMMIT entre sentencias — meter todo en UNA sola sentencia WITH. Escribir en `auth.users` por SQL puede tirar "must be owner of table users" aunque parte del script se aplique. Para cuentas de usuario, mejor la **API admin** (`/auth/v1/admin/users`) con la service_role key: el usuario la comparte por chat desde Project Settings → API Keys (NO guardarla en archivos; así se crearon las 32 cuentas el 2026-07-20). Ojo: rotar el JWT secret invalidaría también la anon key hardcodeada en `src/lib/supabase.ts`.
- **Roles** (`user_roles`, tabla compartida): admite `admin` (común a ambos sistemas), `medico`, `recepcion`. Los roles de control de peso (`analyst`/`viewer`) ven pantalla "Sin acceso" (gate en `ProtectedRoute` + `ROLES_SANIDAD` en `auth-context`). No se auto-asigna rol.
- **Permisos**: `user_roles.permissions` = `Record<moduleKey, ('ver'|'editar'|'exportar'|'eliminar')[]>`. Módulos: `pacientes`, `citas`, `consultas`, `recetas`, `lista_espera`, `reportes`, `mantenimiento`, `usuarios`. Defaults por rol en `DEFAULT_PERMISOS_SANIDAD` (auth-context). OJO: la columna `permissions` tiene DEFAULT `'{}'::jsonb` y el fallback usa `?? DEFAULT` que NO cubre `{}` (solo null); al crear usuarios por SQL hay que setear `permissions` explícito o quedan sin módulos (solo Dashboard).
- **RLS**: políticas `sanidad_*` (lectura para personal activo; insert/update en pacientes/citas/consultas/lista_espera/recetas; delete solo admin; gestión de catálogos medicos/especialidades/cie10 solo admin). Funciones `es_sanidad_activo()` / `es_sanidad_admin()`.
- **Capa de datos**: `src/api/<dominio>.ts` con funciones fetch + hooks de React Query; keys en `src/lib/query-client.ts`. Soft-delete (`activo:false`) en vez de DELETE. Páginas en `src/pages/*.tsx`, diálogos/forms en `src/components/`.
- **Postgres vs MySQL**: la BD es Postgres. En scripts, `where('activo', true)` de Laravel genera `= 1` y falla; usar SQL crudo con `WHERE activo`. Búsquedas con `ilike`. CLINICA_ID=1 hardcodeado (institución única).
- `_descartado/` = código de control de peso quitado de la plantilla (ignorado por git/tsc/eslint/vitest); borrable.
- storageKey de auth: `sanidad-citas-<ref>-auth-v1` (namespaced para no chocar con control de peso en localhost).

## Relación con Control-Peso

Ambas apps comparten la misma base Supabase. Las consultas que registra un médico aquí alimentan la pestaña **Sanidad** en la ficha del cadete de Control-Peso (vistas `sanidad_resumen_cadete` y `sanidad_historial_cadete`, solo visibles a admins por RLS). No comparten código, solo datos.

Además (2026-07-18): el formulario de consulta permite indicar **reposo** (`consultas.reposo_tipo` 'local'|'domiciliario' + `reposo_desde`/`reposo_hasta`; NULL = sin reposo). Eso alimenta la página **Actividad Física** de Control-Peso vía la vista `sanidad_reposos_cadete` (solo admins). Migración en `..\Control-Peso\supabase\migrations\20260718_reposos_actividad_fisica.sql` (aplicada).
